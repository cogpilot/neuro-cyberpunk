#include <semver.hpp>

namespace build
{
constexpr semver::version<> GetModVersion()
{
    semver::version ver{};
    semver::parse("${VERSION}", ver);
    return ver;
}

inline constexpr auto AuthorName = L"${AUTHOR_NAME}";
inline constexpr auto ProjectName = L"${NAME}";
}
